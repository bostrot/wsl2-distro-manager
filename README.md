# Community catalogue

`cdn/scripts.json` is the community script catalogue the app reads: every
`info.yml` from [bostrot/wsl-scripts](https://github.com/bostrot/wsl-scripts)
collected into one file. The `Community catalogue` workflow on `main`
regenerates it daily and pushes here; nothing on this branch is edited by hand.
